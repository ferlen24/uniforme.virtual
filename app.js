const video=document.getElementById('video');const startBtn=document.getElementById('startBtn');const shotBtn=document.getElementById('shotBtn');const cameraView=document.getElementById('cameraView');const result=document.getElementById('result');const canvas=document.getElementById('canvas');const downloadBtn=document.getElementById('downloadBtn');const againBtn=document.getElementById('againBtn');let stream=null;async function startCamera(){try{stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:{ideal:'environment'},width:{ideal:1280},height:{ideal:1920}},audio:false});video.srcObject=stream;await video.play();startBtn.classList.add('hidden');shotBtn.disabled=false}catch(e){alert('No se pudo abrir la cámara. Permití el acceso e intentá nuevamente.')}}function drawUniform(ctx,w,h){ctx.save();const cx=w/2;const top=h*.43;const uw=w*.78;const uh=h*.52;ctx.fillStyle='#5f6870';ctx.beginPath();ctx.moveTo(cx-uw*.18,top);ctx.lineTo(cx-uw*.48,top+uh*.18);ctx.lineTo(cx-uw*.38,top+uh);ctx.lineTo(cx+uw*.38,top+uh);ctx.lineTo(cx+uw*.48,top+uh*.18);ctx.lineTo(cx+uw*.18,top);ctx.closePath();ctx.fill();ctx.fillStyle='#d7b85a';ctx.fillRect(cx-uw*.31,top+uh*.12,uw*.2,Math.max(4,w*.012));ctx.fillRect(cx+uw*.11,top+uh*.12,uw*.2,Math.max(4,w*.012));ctx.font=`${Math.round(w*.07)}px sans-serif`;ctx.fillText('★',cx+uw*.18,top+uh*.32);ctx.restore()}function takePhoto(){const w=video.videoWidth,h=video.videoHeight;if(!w||!h)return;canvas.width=w;canvas.height=h;const ctx=canvas.getContext('2d');ctx.drawImage(video,0,0,w,h);drawUniform(ctx,w,h);downloadBtn.href=canvas.toDataURL('image/jpeg',.92);cameraView.classList.add('hidden');shotBtn.classList.add('hidden');result.classList.remove('hidden')}function again(){result.classList.add('hidden');cameraView.classList.remove('hidden');shotBtn.classList.remove('hidden')}startBtn.addEventListener('click',startCamera);shotBtn.addEventListener('click',takePhoto);againBtn.addEventListener('click',again);